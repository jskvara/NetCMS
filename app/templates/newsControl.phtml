{ifset $news}
{foreach $news as $item}
  <div>
    <h3>{$item->getTitle()} ({$item->getCreated()})</h3>
    {!$item->getText()}
  </div>
{/foreach}
{widget paginator}
{else}
  <p>Nebyly přidány žádné novinky</p>
{/if}
