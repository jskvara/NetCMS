{var $showForm true}
{foreach $flashes as $flash}
    {*{if $flash->type === "emailing"}*}
        {$showForm = false}
        <div class="notice">{$flash->message}</div>
    {*{/if}*}
{/foreach}

{if $showForm === true}
{widget emailingForm}
{/if}